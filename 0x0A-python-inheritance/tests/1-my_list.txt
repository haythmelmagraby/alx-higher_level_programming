import function from module:
        >>> MyList = __import__('1-my_list').MyList

Test correct typing:
        >>> ls = MyList()
        >>> type(ls) == MyList
	True

Test correct inhirit:
        >>> ls = MyList()
        >>> isinstance(ls, list)
        True
